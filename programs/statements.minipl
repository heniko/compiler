program hello_world;

procedure plus_one(i:integer);
begin
  i := i + 1;
end;

procedure plus_one(i:integer, arr:array [] of integer);
begin
  i := i + 1;
end;

begin
  var i : integer;
  plus_one(i);
end.